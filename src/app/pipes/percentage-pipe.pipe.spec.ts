import { PercentagePipe } from './percentage-pipe.pipe';

describe('PercentagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
